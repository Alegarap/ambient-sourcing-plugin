# 1Password-backed .env — secrets are op:// references, resolved at runtime.
# Usage: op run --env-file=.env.1password -- claude
# Safe to commit — no actual secrets.

# Supabase (Ambient Sourcing DB)
SUPABASE_URL=https://mmzcyfwklvugqerwczzm.supabase.co
SUPABASE_SERVICE_ROLE_KEY="op://lunar-secrets/Supabase Ambient Sourcing/service role key"

# OpenRouter (for semantic search embedding generation)
OPENROUTER_API_KEY="op://lunar-secrets/Supabase Ambient Sourcing/OpenRouter Ambient Sourcing CC Plugin"
